# Dotfiles

feenko's archlinux dotfiles managed with [chezmoi](https://github.com/twpayne/chezmoi).

## Installation

```bash
% chezmoi init --ssh --apply codeberg.org/feenko/dotfiles
```

> [!TIP]
> To exclude encrypted files, add flag `--exclude=encrypted`
